[project]
name = "python-boilerplate"
version = "0.1.0"
description = "A Python boilerplate project."
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["marimo>=0.18.4"]

[dependency-groups]
dev = ["ruff>=0.14.10", "ty>=0.0.8", "pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
